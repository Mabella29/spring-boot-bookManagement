spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/book
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  sql:
    init:
      mode: always


  flyway:
    schemas: book_management
